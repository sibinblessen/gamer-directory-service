server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: admin
    password: admin
    initialization-mode: always
  h2:
    console:
      enabled: true

